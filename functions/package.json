{
  "name": "hsmocap-functions",
  "private": true,
  "main": "lib/index.js",
  "type": "module",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "deploy": "firebase deploy --only functions",
    "seed:community": "node ./scripts/seed-community-showcase.mjs",
    "seed:words": "node ./scripts/replace-words-from-seed-admin.mjs",
    "serve": "npm run build && firebase emulators:start --only functions",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^7.2.5",
    "groq-sdk": "^0.30.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
